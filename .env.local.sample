# Optional owner reference; desktop users configure the URL in Settings instead.
# Convex CLI fills CONVEX_DEPLOYMENT and CONVEX_URL during owner setup.
# Do not overwrite an existing .env.local with this sample.
CONVEX_URL=
